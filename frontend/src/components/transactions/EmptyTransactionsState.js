export default function EmptyTransactionsState() {
  return (
    <div className="rounded-lg border border-dashed border-gray-700 bg-[#0b0f19] p-8 text-center">
      <p className="text-sm text-gray-300">Nenhum lancamento encontrado.</p>
      <p className="mt-1 text-xs text-gray-500">Adicione seu primeiro lancamento no formulario ao lado.</p>
    </div>
  );
}
